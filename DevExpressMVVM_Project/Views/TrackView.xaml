<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" 
             xmlns:ViewModels="clr-namespace:DevExpressMVVM_Project.ViewModels"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" 
             x:Class="DevExpressMVVM_Project.Views.TrackView"
             mc:Ignorable="d"
             DataContext="{dxmvvm:ViewModelSource Type={x:Type ViewModels:TrackViewModel}}"
             d:DesignHeight="300" d:DesignWidth="300">

    <StackPanel>
        <Button Content="Clear"
                Command="{Binding ResetNameCommand}"/>
        <dxe:TextEdit Text="{Binding Track.TrackId, Mode=TwoWay}"/>
        <dxe:TextEdit Text="{Binding Track.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <dxe:TextEdit Text="{Binding Track.Composer, Mode=TwoWay}"/>

    </StackPanel>
</UserControl>